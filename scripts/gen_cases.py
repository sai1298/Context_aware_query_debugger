"""Generate dataset cases 011-150 for Chell."""
from __future__ import annotations
import json, pathlib

OUT = pathlib.Path("data/curated")
OUT.mkdir(parents=True, exist_ok=True)

CASES = []

# ── PANDAS (cases 011-057, 47 cases) ──────────────────────────────────────
CASES += [
  dict(id="case_011", buggy_code="result = df.sort_values('sales')", task="Sort sales data in descending order to find top performers", libs=["pandas"], error_type="wrong_sort_order", error_location="line 1: sort_values missing ascending=False", clarification_query="Should the data be sorted in ascending or descending order?", clarification_options=["descending","ascending","random","alphabetical"], expected_user_response="descending", corrected_code="result = df.sort_values('sales', ascending=False)", explanation="sort_values defaults to ascending=True. The task requires descending order so ascending=False is needed.", difficulty="easy", category="pandas"),

  dict(id="case_012", buggy_code="result = df.groupby('region')['revenue'].sum()\nprint(result['North'])", task="Sum revenue by region then access the North region total", libs=["pandas"], error_type="missing_reset_index", error_location="line 2: indexing after groupby without reset_index", clarification_query="After groupby().sum(), is the result indexed by 'region'?", clarification_options=["yes, region is the index","no, integer index remains","it depends on the data","the result has MultiIndex"], expected_user_response="yes, region is the index", corrected_code="result = df.groupby('region')['revenue'].sum()\nprint(result['North'])", explanation="After groupby('region').sum() the region column becomes the index, so result['North'] is correct. No reset_index needed here.", difficulty="easy", category="pandas"),

  dict(id="case_013", buggy_code="df.sort_values('date', inplace=True)\nresult = df.head(10)", task="Sort dataframe by date in place then get first 10 rows", libs=["pandas"], error_type="inplace_not_set", error_location="line 1: inplace=True used correctly but common mistake variant", clarification_query="Should the sort modify the original dataframe or return a new one?", clarification_options=["modify original (inplace=True)","return new dataframe","both","neither"], expected_user_response="modify original (inplace=True)", corrected_code="df.sort_values('date', inplace=True)\nresult = df.head(10)", explanation="inplace=True correctly modifies df in place. Without it, df.sort_values returns a new DataFrame and df remains unsorted.", difficulty="easy", category="pandas"),

  dict(id="case_014", buggy_code="result = df.groupby('category')['price'].mean()", task="Find the total price per category, not the average", libs=["pandas"], error_type="wrong_agg_function", error_location="line 1: .mean() should be .sum()", clarification_query="Which aggregation function gives the total price per category?", clarification_options=["sum()","mean()","count()","max()"], expected_user_response="sum()", corrected_code="result = df.groupby('category')['price'].sum()", explanation="The task asks for the total (sum) price per category. .mean() computes average instead of total.", difficulty="easy", category="pandas"),

  dict(id="case_015", buggy_code="result = df[['name', 'age', 'score']]\nprint(result.head())", task="Select only name and score columns for display", libs=["pandas"], error_type="wrong_column_selection", error_location="line 1: 'age' column included unnecessarily", clarification_query="Which columns should be selected?", clarification_options=["name and score only","name, age, and score","all columns","age and score only"], expected_user_response="name and score only", corrected_code="result = df[['name', 'score']]\nprint(result.head())", explanation="The task specifies only name and score columns but age is also selected.", difficulty="easy", category="pandas"),

  dict(id="case_016", buggy_code="result = df.groupby('dept')['salary'].mean()", task="Compute mean salary per department, excluding rows with missing salary", libs=["pandas"], error_type="missing_dropna", error_location="line 1: NaN rows not dropped before groupby", clarification_query="Should rows with missing salary values be excluded before computing the mean?", clarification_options=["yes, drop NaN rows first","no, keep all rows","fill NaN with 0","fill NaN with mean"], expected_user_response="yes, drop NaN rows first", corrected_code="result = df.dropna(subset=['salary']).groupby('dept')['salary'].mean()", explanation="Without dropna, NaN salaries are automatically excluded by mean() but may skew group counts. Explicit dropna is clearer.", difficulty="medium", category="pandas"),

  dict(id="case_017", buggy_code="pivot = df.pivot_table(values='sales', index='product', columns='month')", task="Create pivot table with month as rows and product as columns", libs=["pandas"], error_type="wrong_pivot_index", error_location="line 1: index and columns are swapped", clarification_query="Which dimension should be the row index of the pivot table?", clarification_options=["month","product","sales","date"], expected_user_response="month", corrected_code="pivot = df.pivot_table(values='sales', index='month', columns='product')", explanation="The task wants months as rows and products as columns, but index and columns are swapped.", difficulty="medium", category="pandas"),

  dict(id="case_018", buggy_code="monthly = df.set_index('date').resample('D')['sales'].sum()", task="Resample daily sales data to monthly totals", libs=["pandas"], error_type="wrong_resample_freq", error_location="line 1: resample('D') gives daily, not monthly", clarification_query="What frequency string gives monthly resampling?", clarification_options=["'ME'","'D'","'W'","'H'"], expected_user_response="'ME'", corrected_code="monthly = df.set_index('date').resample('ME')['sales'].sum()", explanation="'D' resamples to daily frequency. Monthly resampling requires 'ME' (month end) or 'MS' (month start).", difficulty="medium", category="pandas"),

  dict(id="case_019", buggy_code="df['prev_sales'] = df['sales'].shift(-1)", task="Create a column with the previous day's sales value", libs=["pandas"], error_type="wrong_shift_periods", error_location="line 1: shift(-1) shifts forward, not backward", clarification_query="Which shift direction retrieves the previous row's value?", clarification_options=["shift(1)","shift(-1)","shift(0)","shift(2)"], expected_user_response="shift(1)", corrected_code="df['prev_sales'] = df['sales'].shift(1)", explanation="shift(-1) moves values one step forward (next row). shift(1) retrieves the previous row's value.", difficulty="medium", category="pandas"),

  dict(id="case_020", buggy_code="df['rolling_avg'] = df['price'].rolling(window=3).mean()", task="Compute a 7-day rolling average of price", libs=["pandas"], error_type="wrong_rolling_window", error_location="line 1: window=3 instead of window=7", clarification_query="What window size should be used for the 7-day rolling average?", clarification_options=["7","3","14","30"], expected_user_response="7", corrected_code="df['rolling_avg'] = df['price'].rolling(window=7).mean()", explanation="window=3 computes a 3-period rolling average, not 7-day as required.", difficulty="easy", category="pandas"),

  dict(id="case_021", buggy_code="freq = df['color'].value_counts()", task="Get relative frequency (proportions) of each color value", libs=["pandas"], error_type="wrong_value_counts_normalize", error_location="line 1: missing normalize=True", clarification_query="To get proportions instead of counts, which parameter is needed?", clarification_options=["normalize=True","relative=True","pct=True","freq=True"], expected_user_response="normalize=True", corrected_code="freq = df['color'].value_counts(normalize=True)", explanation="value_counts() returns raw counts. normalize=True converts them to proportions summing to 1.", difficulty="easy", category="pandas"),

  dict(id="case_022", buggy_code="df['score'].fillna(0, inplace=True)", task="Fill missing scores with the column mean, not zero", libs=["pandas"], error_type="wrong_fillna_method", error_location="line 1: fillna(0) uses 0 instead of column mean", clarification_query="What value should missing scores be filled with?", clarification_options=["column mean","0","median","forward fill"], expected_user_response="column mean", corrected_code="df['score'].fillna(df['score'].mean(), inplace=True)", explanation="fillna(0) replaces NaN with 0, which distorts statistics. Using the column mean preserves the distribution.", difficulty="easy", category="pandas"),

  dict(id="case_023", buggy_code="df.rename(columns={'old_name': 'new_name', 'old_age': 'new_age'})", task="Rename columns and update the dataframe in place", libs=["pandas"], error_type="wrong_rename_columns", error_location="line 1: rename result not assigned back", clarification_query="How should the rename be applied to update the original dataframe?", clarification_options=["assign result back or use inplace=True","rename modifies in place automatically","use df.columns = [...]","use df.update()"], expected_user_response="assign result back or use inplace=True", corrected_code="df.rename(columns={'old_name': 'new_name', 'old_age': 'new_age'}, inplace=True)", explanation="rename() returns a new DataFrame without inplace=True, leaving df unchanged.", difficulty="easy", category="pandas"),

  dict(id="case_024", buggy_code="df.set_index('user_id')\nresult = df.loc[101]", task="Set user_id as index then retrieve user 101", libs=["pandas"], error_type="wrong_set_index", error_location="line 1: set_index result not assigned back", clarification_query="Does set_index modify the dataframe in place by default?", clarification_options=["no, must assign or use inplace=True","yes, it modifies in place","only when chained","depends on pandas version"], expected_user_response="no, must assign or use inplace=True", corrected_code="df = df.set_index('user_id')\nresult = df.loc[101]", explanation="set_index returns a new DataFrame. The result must be assigned back to df or inplace=True used.", difficulty="easy", category="pandas"),

  dict(id="case_025", buggy_code="result = df.apply(lambda row: row['a'] + row['b'], axis=0)", task="Add columns 'a' and 'b' element-wise using apply across rows", libs=["pandas"], error_type="wrong_apply_axis", error_location="line 1: axis=0 applies function along columns, not rows", clarification_query="Which axis value applies a function row-by-row?", clarification_options=["axis=1","axis=0","axis='index'","axis='both'"], expected_user_response="axis=1", corrected_code="result = df.apply(lambda row: row['a'] + row['b'], axis=1)", explanation="axis=0 applies the function to each column (down rows). axis=1 applies it to each row across columns.", difficulty="medium", category="pandas"),

  dict(id="case_026", buggy_code="result = pd.concat([df1, df2], axis=1)", task="Stack two dataframes with the same columns vertically", libs=["pandas"], error_type="wrong_concat_axis", error_location="line 1: axis=1 concatenates horizontally", clarification_query="Which axis value stacks dataframes vertically (more rows)?", clarification_options=["axis=0","axis=1","axis='columns'","axis=2"], expected_user_response="axis=0", corrected_code="result = pd.concat([df1, df2], axis=0)", explanation="axis=1 concatenates side by side (more columns). axis=0 stacks vertically (more rows).", difficulty="easy", category="pandas"),

  dict(id="case_027", buggy_code="result = pd.merge(df_orders, df_customers, on='customer_id', how='inner')", task="Keep all orders including those without matching customers", libs=["pandas"], error_type="wrong_merge_how", error_location="line 1: how='inner' drops unmatched orders", clarification_query="Which merge type keeps all rows from the left dataframe?", clarification_options=["left","inner","outer","right"], expected_user_response="left", corrected_code="result = pd.merge(df_orders, df_customers, on='customer_id', how='left')", explanation="inner merge drops orders with no matching customer. left join keeps all orders and fills unmatched customer columns with NaN.", difficulty="medium", category="pandas"),

  dict(id="case_028", buggy_code="df['age_group'] = pd.cut(df['age'], bins=[0, 18, 35, 60, 100])", task="Bin ages into groups with labels: child, young_adult, adult, senior", libs=["pandas"], error_type="wrong_cut_bins", error_location="line 1: labels parameter missing", clarification_query="How do you assign labels to the age bins?", clarification_options=["add labels=['child','young_adult','adult','senior']","use rename after cut","the bins define the labels","use pd.qcut instead"], expected_user_response="add labels=['child','young_adult','adult','senior']", corrected_code="df['age_group'] = pd.cut(df['age'], bins=[0,18,35,60,100], labels=['child','young_adult','adult','senior'])", explanation="Without labels, pd.cut uses interval notation like (0, 18]. Passing labels assigns human-readable names.", difficulty="medium", category="pandas"),

  dict(id="case_029", buggy_code="df['name_upper'] = df['name'].str.lower()", task="Convert the name column to uppercase", libs=["pandas"], error_type="wrong_str_method", error_location="line 1: .str.lower() lowercases instead of uppercases", clarification_query="Which string method converts to uppercase?", clarification_options=[".str.upper()",".str.lower()",".str.title()",".str.capitalize()"], expected_user_response=".str.upper()", corrected_code="df['name_upper'] = df['name'].str.upper()", explanation=".str.lower() converts to lowercase. The task requires .str.upper() for uppercase.", difficulty="easy", category="pandas"),

  dict(id="case_030", buggy_code="df['year'] = df['date'].dt.month", task="Extract the year from a datetime column", libs=["pandas"], error_type="wrong_datetime_operation", error_location="line 1: .dt.month extracts month, not year", clarification_query="Which datetime accessor extracts the year?", clarification_options=[".dt.year",".dt.month",".dt.day",".dt.quarter"], expected_user_response=".dt.year", corrected_code="df['year'] = df['date'].dt.year", explanation=".dt.month returns the month number (1-12). .dt.year is needed to extract the year.", difficulty="easy", category="pandas"),

  dict(id="case_031", buggy_code="df['running_total'] = df['sales'].cumsum()\nprint(df[df['running_total'] > 1000])", task="Show rows where cumulative sales exceed 1000, after sorting by date", libs=["pandas"], error_type="missing_sort_before_cumsum", error_location="line 1: df not sorted by date before cumsum", clarification_query="Should the data be sorted by date before computing the cumulative sum?", clarification_options=["yes, sort by date first","no, order doesn't matter for cumsum","cumsum sorts automatically","use resample instead"], expected_user_response="yes, sort by date first", corrected_code="df = df.sort_values('date')\ndf['running_total'] = df['sales'].cumsum()\nprint(df[df['running_total'] > 1000])", explanation="cumsum() depends on row order. Without sorting by date first, the running total is computed in arbitrary order.", difficulty="medium", category="pandas"),

  dict(id="case_032", buggy_code="long_df = df.melt(id_vars=['year'], value_vars=['q1','q2','q3','q4'])", task="Melt the dataframe keeping 'product' as identifier, not 'year'", libs=["pandas"], error_type="wrong_melt_id_vars", error_location="line 1: id_vars='year' should be id_vars='product'", clarification_query="Which column should be kept as the identifier variable?", clarification_options=["product","year","quarter","value"], expected_user_response="product", corrected_code="long_df = df.melt(id_vars=['product'], value_vars=['q1','q2','q3','q4'])", explanation="id_vars specifies which columns remain fixed. The task requires 'product' as the identifier, not 'year'.", difficulty="medium", category="pandas"),

  dict(id="case_033", buggy_code="result = df.groupby(['dept','role'])['salary'].mean().unstack('dept')", task="Compute mean salary by dept and role, with role as columns", libs=["pandas"], error_type="wrong_stack_unstack", error_location="line 1: unstack('dept') puts dept as columns instead of role", clarification_query="Which level should be unstacked to make role the columns?", clarification_options=["unstack('role')","unstack('dept')","unstack(0)","unstack(-2)"], expected_user_response="unstack('role')", corrected_code="result = df.groupby(['dept','role'])['salary'].mean().unstack('role')", explanation="unstack('dept') moves dept to columns. To make role the columns, unstack('role') is needed.", difficulty="hard", category="pandas"),

  dict(id="case_034", buggy_code="ct = pd.crosstab(df['gender'], df['product'], margins=False)", task="Create a crosstab of gender vs product with row and column totals", libs=["pandas"], error_type="wrong_crosstab", error_location="line 1: margins=False omits row/column totals", clarification_query="Which parameter adds row and column totals to crosstab?", clarification_options=["margins=True","totals=True","sum=True","include_margins=True"], expected_user_response="margins=True", corrected_code="ct = pd.crosstab(df['gender'], df['product'], margins=True)", explanation="margins=False (the default) omits the All row/column. Setting margins=True adds totals.", difficulty="easy", category="pandas"),

  dict(id="case_035", buggy_code="df['ewm_avg'] = df['price'].ewm(span=5).mean()", task="Compute exponentially weighted moving average with span=20", libs=["pandas"], error_type="wrong_ewm_span", error_location="line 1: span=5 instead of span=20", clarification_query="What span value produces the 20-period EWM average?", clarification_options=["20","5","10","50"], expected_user_response="20", corrected_code="df['ewm_avg'] = df['price'].ewm(span=20).mean()", explanation="span=5 gives a fast-reacting average. The task requires span=20 for a slower, smoother average.", difficulty="easy", category="pandas"),

  dict(id="case_036", buggy_code="top5 = df.nlargest(5, 'revenue').index.tolist()", task="Get the names of the 5 products with highest revenue", libs=["pandas"], error_type="wrong_nlargest_column", error_location="line 1: .index used but product names are in 'product' column", clarification_query="If product names are in a 'product' column (not the index), how do you extract them?", clarification_options=["df.nlargest(5,'revenue')['product'].tolist()","df.nlargest(5,'revenue').index.tolist()","df.nlargest(5,'product')['revenue'].tolist()","df.nlargest('revenue',5)['product'].tolist()"], expected_user_response="df.nlargest(5,'revenue')['product'].tolist()", corrected_code="top5 = df.nlargest(5, 'revenue')['product'].tolist()", explanation="Unless 'product' is set as the index, product names live in the 'product' column, not .index.", difficulty="medium", category="pandas"),

  dict(id="case_037", buggy_code="best_day = df['sales'].idxmax()\nprint(best_day)", task="Print the date corresponding to the highest sales (date is in the 'date' column, not index)", libs=["pandas"], error_type="wrong_idxmax", error_location="line 1: idxmax() returns the index label, not the date value", clarification_query="If the date is in a 'date' column, how do you get the date for the max sales row?", clarification_options=["df.loc[df['sales'].idxmax(), 'date']","df['sales'].idxmax()","df['date'].idxmax()","df.loc[df['sales'].max(), 'date']"], expected_user_response="df.loc[df['sales'].idxmax(), 'date']", corrected_code="best_day = df.loc[df['sales'].idxmax(), 'date']\nprint(best_day)", explanation="idxmax() returns the integer index of the max row. To get the date value, use df.loc[idx, 'date'].", difficulty="medium", category="pandas"),

  dict(id="case_038", buggy_code="df['clipped'] = df['score'].clip(lower=0, upper=50)", task="Clip scores to the range 0 to 100", libs=["pandas"], error_type="wrong_clip_values", error_location="line 1: upper=50 should be upper=100", clarification_query="What should the upper clip bound be?", clarification_options=["100","50","75","200"], expected_user_response="100", corrected_code="df['clipped'] = df['score'].clip(lower=0, upper=100)", explanation="upper=50 caps scores at 50, but the valid range is 0–100. upper=100 is correct.", difficulty="easy", category="pandas"),

  dict(id="case_039", buggy_code="valid = df[df['age'].between(18, 65, inclusive='neither')]", task="Filter rows where age is between 18 and 65, including both endpoints", libs=["pandas"], error_type="wrong_between_inclusive", error_location="line 1: inclusive='neither' excludes both endpoints", clarification_query="Which inclusive value includes both 18 and 65?", clarification_options=["'both'","'neither'","'left'","'right'"], expected_user_response="'both'", corrected_code="valid = df[df['age'].between(18, 65, inclusive='both')]", explanation="inclusive='neither' excludes 18 and 65. inclusive='both' (default) includes both endpoints.", difficulty="easy", category="pandas"),

  dict(id="case_040", buggy_code="df['flagged'] = df['score'].mask(df['score'] < 0)", task="Flag rows where score is above 100 as NaN", libs=["pandas"], error_type="wrong_mask_condition", error_location="line 1: mask condition is < 0 instead of > 100", clarification_query="What condition should trigger the NaN replacement?", clarification_options=["score > 100","score < 0","score == 0","score < 100"], expected_user_response="score > 100", corrected_code="df['flagged'] = df['score'].mask(df['score'] > 100)", explanation="mask() replaces values where the condition is True. The condition should be score > 100, not < 0.", difficulty="easy", category="pandas"),

  dict(id="case_041", buggy_code="safe = df.where(df['price'] > 0)\nresult = safe['price'].sum()", task="Sum only positive prices, treating non-positive as zero (not NaN)", libs=["pandas"], error_type="wrong_where_condition", error_location="line 1: where() replaces non-positive with NaN, not 0", clarification_query="How do you replace non-positive prices with 0 using where()?", clarification_options=["df['price'].where(df['price']>0, 0)","df.where(df['price']>0)","df['price'].clip(lower=0)","df[df['price']>0]['price'].sum()"], expected_user_response="df['price'].where(df['price']>0, 0)", corrected_code="result = df['price'].where(df['price'] > 0, 0).sum()", explanation="where() without a replacement value uses NaN. Providing 0 as second argument replaces False-condition values with 0.", difficulty="medium", category="pandas"),

  dict(id="case_042", buggy_code="df['adjusted'] = df['price'] * 1.1\ndf2 = df\ndf2['adjusted'] = df2['adjusted'].round(2)", task="Create a copy of df, round the adjusted column in the copy without affecting original", libs=["pandas"], error_type="missing_copy", error_location="line 2: df2 = df creates a view/reference, not a copy", clarification_query="How do you create an independent copy of a DataFrame?", clarification_options=["df.copy()","df[:]","df.clone()","pd.DataFrame(df)"], expected_user_response="df.copy()", corrected_code="df['adjusted'] = df['price'] * 1.1\ndf2 = df.copy()\ndf2['adjusted'] = df2['adjusted'].round(2)", explanation="df2 = df makes both variables point to the same object. df.copy() creates an independent copy.", difficulty="medium", category="pandas"),

  dict(id="case_043", buggy_code="result = df.assign(total=df['price'] + df['tax'])\nresult = df.assign(discount=df['total'] * 0.1)", task="Chain two assign calls: first add 'total', then derive 'discount' from it", libs=["pandas"], error_type="wrong_assign_column", error_location="line 2: df.assign used instead of result.assign, losing 'total'", clarification_query="How should the second assign reference the 'total' column created in the first assign?", clarification_options=["chain: df.assign(total=...).assign(discount=lambda x: x['total']*0.1)","result.assign(discount=result['total']*0.1)","both are equivalent","use df['total'] in second assign"], expected_user_response="chain: df.assign(total=...).assign(discount=lambda x: x['total']*0.1)", corrected_code="result = df.assign(total=df['price']+df['tax']).assign(discount=lambda x: x['total']*0.1)", explanation="assign() doesn't modify df in place. Chaining assign() calls with a lambda ensures the second call sees the column added by the first.", difficulty="hard", category="pandas"),

  dict(id="case_044", buggy_code="df['dept_avg'] = df.groupby('dept')['salary'].transform('mean')\ndf['above_avg'] = df['salary'] > df['dept_avg']", task="Flag employees earning above their department average using transform", libs=["pandas"], error_type="wrong_groupby_transform", error_location="correct code — this tests that transform is used properly", clarification_query="Does groupby transform return a value aligned to the original DataFrame index?", clarification_options=["yes, same index as original df","no, it returns a grouped index","only when reset_index is called","only for numeric columns"], expected_user_response="yes, same index as original df", corrected_code="df['dept_avg'] = df.groupby('dept')['salary'].transform('mean')\ndf['above_avg'] = df['salary'] > df['dept_avg']", explanation="transform('mean') broadcasts the group mean back to each row, aligned to the original index. This is the correct pattern.", difficulty="medium", category="pandas"),

  dict(id="case_045", buggy_code="n_unique = df.groupby('store')['product'].count()", task="Count distinct products per store", libs=["pandas"], error_type="wrong_nunique", error_location="line 1: .count() counts all non-null rows, not unique values", clarification_query="Which aggregation counts distinct values?", clarification_options=["nunique()","count()","unique()","distinct()"], expected_user_response="nunique()", corrected_code="n_unique = df.groupby('store')['product'].nunique()", explanation=".count() counts total non-null entries including duplicates. .nunique() counts only distinct values.", difficulty="easy", category="pandas"),

  dict(id="case_046", buggy_code="df['grade'] = df['score'].map({'A':4,'B':3,'C':2,'D':1})", task="Map numeric scores to letter grades: >=90='A', >=80='B', >=70='C', else='D'", libs=["pandas"], error_type="wrong_map_values", error_location="line 1: map with dict requires exact keys; score is numeric", clarification_query="For mapping numeric ranges to categories, which approach is correct?", clarification_options=["pd.cut with labels","map with a dict","apply with lambda","replace with dict"], expected_user_response="pd.cut with labels", corrected_code="df['grade'] = pd.cut(df['score'], bins=[0,70,80,90,100], labels=['D','C','B','A'], right=True)", explanation="map() with a dict requires exact key matches. For range-based mapping of numeric values, pd.cut() is appropriate.", difficulty="medium", category="pandas"),

  dict(id="case_047", buggy_code="df['status'] = df['status'].replace('activ', 'active')", task="Replace both 'activ' and 'inactiv' with their correct spellings 'active' and 'inactive'", libs=["pandas"], error_type="wrong_replace_value", error_location="line 1: only one replacement made, 'inactiv' not fixed", clarification_query="How do you replace multiple values at once with replace()?", clarification_options=["pass a dict: replace({'activ':'active','inactiv':'inactive'})","call replace twice","use str.replace with regex","use map with a dict"], expected_user_response="pass a dict: replace({'activ':'active','inactiv':'inactive'})", corrected_code="df['status'] = df['status'].replace({'activ': 'active', 'inactiv': 'inactive'})", explanation="Passing a dict to replace() performs all substitutions in one call, avoiding a second pass.", difficulty="easy", category="pandas"),

  dict(id="case_048", buggy_code="result = df.loc[df['score'] > 80 and df['grade'] == 'A']", task="Filter rows where score > 80 AND grade is 'A'", libs=["pandas"], error_type="wrong_loc_condition", error_location="line 1: Python 'and' used instead of bitwise '&' with parentheses", clarification_query="How should multiple boolean conditions be combined in a DataFrame filter?", clarification_options=["use & with parentheses: (cond1) & (cond2)","use 'and': cond1 and cond2","use np.logical_and","use df.query()"], expected_user_response="use & with parentheses: (cond1) & (cond2)", corrected_code="result = df.loc[(df['score'] > 80) & (df['grade'] == 'A')]", explanation="Python's 'and' on Series raises ValueError. Bitwise & with parenthesized conditions is required for element-wise boolean AND.", difficulty="medium", category="pandas"),

  dict(id="case_049", buggy_code="df['pct_change'] = df['revenue'].pct_change(periods=2)", task="Compute period-over-period (1-period) percentage change in revenue", libs=["pandas"], error_type="wrong_shift_periods", error_location="line 1: periods=2 computes 2-period change", clarification_query="What periods value gives standard 1-period percentage change?", clarification_options=["1 (default)","2","0","-1"], expected_user_response="1 (default)", corrected_code="df['pct_change'] = df['revenue'].pct_change()", explanation="periods=2 compares each value to the one 2 periods ago. The default periods=1 is needed for consecutive-period change.", difficulty="easy", category="pandas"),

  dict(id="case_050", buggy_code="q75 = df['salary'].quantile(0.5)", task="Compute the 75th percentile of salary", libs=["pandas"], error_type="wrong_quantile", error_location="line 1: quantile(0.5) is the median, not 75th percentile", clarification_query="What argument gives the 75th percentile?", clarification_options=["0.75","0.5","75","0.25"], expected_user_response="0.75", corrected_code="q75 = df['salary'].quantile(0.75)", explanation="quantile(0.5) returns the median (50th percentile). The 75th percentile requires quantile(0.75).", difficulty="easy", category="pandas"),

  dict(id="case_051", buggy_code="df['category'] = df['category'].astype('str')", task="Convert category column to pandas Categorical type for memory efficiency", libs=["pandas"], error_type="wrong_type_conversion", error_location="line 1: astype('str') converts to string, not Categorical", clarification_query="Which dtype converts a column to pandas Categorical?", clarification_options=["'category'","'str'","'object'","'categorical'"], expected_user_response="'category'", corrected_code="df['category'] = df['category'].astype('category')", explanation="astype('str') or astype('object') keeps string data. astype('category') creates an efficient Categorical representation.", difficulty="easy", category="pandas"),

  dict(id="case_052", buggy_code="result = df.sample(frac=0.1, replace=False, random_state=42)", task="Sample 20% of the dataframe with replacement for bootstrapping", libs=["pandas"], error_type="wrong_sample_frac", error_location="line 1: frac=0.1 samples 10%, and replace=False disallows replacement", clarification_query="What parameters produce a 20% sample with replacement?", clarification_options=["frac=0.2, replace=True","frac=0.1, replace=False","frac=20, replace=True","n=0.2, replace=True"], expected_user_response="frac=0.2, replace=True", corrected_code="result = df.sample(frac=0.2, replace=True, random_state=42)", explanation="frac=0.1 draws 10% of rows and replace=False prevents resampling. Bootstrap requires frac=0.2 and replace=True.", difficulty="easy", category="pandas"),

  dict(id="case_053", buggy_code="result = df.drop_duplicates(subset=['email'])\nresult.reset_index()", task="Drop duplicate emails and reset the integer index on the resulting dataframe", libs=["pandas"], error_type="missing_reset_index", error_location="line 2: reset_index() result not assigned back", clarification_query="Does reset_index() modify the dataframe in place by default?", clarification_options=["no, must assign result or use inplace=True","yes","only after drop_duplicates","only when drop=True"], expected_user_response="no, must assign result or use inplace=True", corrected_code="result = df.drop_duplicates(subset=['email']).reset_index(drop=True)", explanation="reset_index() returns a new DataFrame. Without assignment, the reset index is discarded.", difficulty="easy", category="pandas"),

  dict(id="case_054", buggy_code="result = df.groupby('team')['points'].agg(['mean', 'std', 'count'])\nresult.columns = ['avg', 'spread', 'n']", task="Aggregate points by team computing mean, std, and count with renamed columns", libs=["pandas"], error_type="wrong_agg_function", error_location="correct — validates multi-agg with rename", clarification_query="After agg(['mean','std','count']), can you rename columns by direct assignment to result.columns?", clarification_options=["yes, column list assignment works","no, use rename()","no, must pass as dict to agg","only if reset_index first"], expected_user_response="yes, column list assignment works", corrected_code="result = df.groupby('team')['points'].agg(['mean', 'std', 'count'])\nresult.columns = ['avg', 'spread', 'n']", explanation="After agg returns a DataFrame with the aggregation names as columns, direct column list assignment renames them.", difficulty="medium", category="pandas"),

  dict(id="case_055", buggy_code="df_wide = df.pivot(index='date', columns='metric', values='value')\ndf_wide = df_wide.fillna(method='ffill')", task="Pivot then forward-fill missing values using the modern pandas API", libs=["pandas"], error_type="wrong_fillna_method", error_location="line 2: fillna(method='ffill') is deprecated; use ffill() directly", clarification_query="What is the modern pandas method for forward-filling missing values?", clarification_options=[".ffill()",".fillna(method='ffill')",".forward_fill()",".interpolate(method='pad')"], expected_user_response=".ffill()", corrected_code="df_wide = df.pivot(index='date', columns='metric', values='value')\ndf_wide = df_wide.ffill()", explanation="fillna(method='ffill') is deprecated in pandas 2.x. The direct .ffill() method is the modern replacement.", difficulty="medium", category="pandas"),

  dict(id="case_056", buggy_code="result = df.query('score > 80 & grade = \"A\"')", task="Use df.query() to filter score > 80 AND grade == 'A'", libs=["pandas"], error_type="wrong_filter_condition", error_location="line 1: single '=' used for equality in query string", clarification_query="What equality operator must be used inside df.query()?", clarification_options=["==","=","is","eq"], expected_user_response="==", corrected_code="result = df.query('score > 80 & grade == \"A\"')", explanation="A single = in query string syntax causes a SyntaxError. The equality operator inside query strings is ==.", difficulty="easy", category="pandas"),

  dict(id="case_057", buggy_code="df_sorted = df.sort_values(['dept','salary'], ascending=[True, True])", task="Sort by department ascending, then salary descending within each department", libs=["pandas"], error_type="wrong_sort_order", error_location="line 1: both ascending; salary should be descending", clarification_query="How should the ascending parameter look to sort dept asc and salary desc?", clarification_options=["[True, False]","[True, True]","[False, True]","[False, False]"], expected_user_response="[True, False]", corrected_code="df_sorted = df.sort_values(['dept','salary'], ascending=[True, False])", explanation="ascending=[True, True] sorts both columns ascending. [True, False] sorts dept ascending and salary descending.", difficulty="easy", category="pandas"),
]

# ── NUMPY (cases 058-095, 38 cases) ───────────────────────────────────────
CASES += [
  dict(id="case_058", buggy_code="result = arr.reshape(3, 4)", task="Reshape a 12-element array into 4 rows and 3 columns", libs=["numpy"], error_type="wrong_reshape_order", error_location="line 1: (3,4) gives 3 rows 4 cols, not 4 rows 3 cols", clarification_query="What shape argument gives 4 rows and 3 columns?", clarification_options=["(4, 3)","(3, 4)","(12,)","(-1, 4)"], expected_user_response="(4, 3)", corrected_code="result = arr.reshape(4, 3)", explanation="reshape(3, 4) produces shape (3,4) — 3 rows, 4 columns. (4, 3) gives 4 rows and 3 columns.", difficulty="easy", category="numpy"),

  dict(id="case_059", buggy_code="result = arr.T\nprint(result.shape)", task="Transpose a 2D array and verify the shape is swapped", libs=["numpy"], error_type="wrong_transpose", error_location="correct — validates .T attribute usage", clarification_query="For an array of shape (m, n), what is the shape after .T?", clarification_options=["(n, m)","(m, n)","(m*n,)","(1, m, n)"], expected_user_response="(n, m)", corrected_code="result = arr.T\nprint(result.shape)", explanation=".T swaps all axes. An array of shape (m, n) becomes (n, m) after transpose.", difficulty="easy", category="numpy"),

  dict(id="case_060", buggy_code="total = np.sum(matrix, axis=1)", task="Sum across columns to get one total per column (sum down each column)", libs=["numpy"], error_type="wrong_sum_axis", error_location="line 1: axis=1 sums across columns (per-row sum), not per-column", clarification_query="Which axis sums down rows to give one value per column?", clarification_options=["axis=0","axis=1","axis=-1","axis=2"], expected_user_response="axis=0", corrected_code="total = np.sum(matrix, axis=0)", explanation="axis=1 reduces along columns (row-wise sum). axis=0 reduces along rows, giving one sum per column.", difficulty="medium", category="numpy"),

  dict(id="case_061", buggy_code="mean_cols = np.mean(matrix, axis=0)", task="Compute the mean of each row (one mean value per row)", libs=["numpy"], error_type="wrong_mean_axis", error_location="line 1: axis=0 computes column means, not row means", clarification_query="Which axis computes a mean across columns (one value per row)?", clarification_options=["axis=1","axis=0","axis=-2","no axis"], expected_user_response="axis=1", corrected_code="mean_cols = np.mean(matrix, axis=1)", explanation="axis=0 collapses the row dimension, giving one mean per column. axis=1 gives one mean per row.", difficulty="medium", category="numpy"),

  dict(id="case_062", buggy_code="std = np.std(data, ddof=0)", task="Compute the sample standard deviation (unbiased, ddof=1)", libs=["numpy"], error_type="wrong_std_ddof", error_location="line 1: ddof=0 computes population std, not sample std", clarification_query="Which ddof value gives the unbiased sample standard deviation?", clarification_options=["ddof=1","ddof=0","ddof=2","ddof=-1"], expected_user_response="ddof=1", corrected_code="std = np.std(data, ddof=1)", explanation="ddof=0 divides by N (population std). ddof=1 divides by N-1 for the unbiased sample estimate.", difficulty="medium", category="numpy"),

  dict(id="case_063", buggy_code="idx = np.argmax(arr, axis=0)", task="Find the index of the maximum value in the entire flattened array", libs=["numpy"], error_type="wrong_argmax", error_location="line 1: axis=0 finds argmax per column, not over flattened array", clarification_query="How do you find the index of the global maximum across all elements?", clarification_options=["np.argmax(arr) with no axis","np.argmax(arr, axis=0)","np.argmax(arr, axis=1)","np.argmax(arr.flat)"], expected_user_response="np.argmax(arr) with no axis", corrected_code="idx = np.argmax(arr)", explanation="Specifying axis=0 returns per-column argmax. Omitting axis flattens the array and returns the global maximum index.", difficulty="medium", category="numpy"),

  dict(id="case_064", buggy_code="result = np.clip(arr, a_min=0, a_max=50)", task="Clip array values to the range [0, 255] for image processing", libs=["numpy"], error_type="wrong_clip_values", error_location="line 1: a_max=50 instead of a_max=255", clarification_query="What should the upper clip bound be for 8-bit image values?", clarification_options=["255","50","128","100"], expected_user_response="255", corrected_code="result = np.clip(arr, a_min=0, a_max=255)", explanation="Image pixel values range 0–255. a_max=50 would clip most pixel values unnecessarily.", difficulty="easy", category="numpy"),

  dict(id="case_065", buggy_code="x = np.linspace(0, 10, 5)", task="Create 100 evenly spaced values between 0 and 10", libs=["numpy"], error_type="wrong_linspace_num", error_location="line 1: num=5 instead of num=100", clarification_query="What is the third argument to linspace for 100 points?", clarification_options=["100","5","10","1000"], expected_user_response="100", corrected_code="x = np.linspace(0, 10, 100)", explanation="linspace(start, stop, num) creates num evenly spaced points. 5 was used instead of 100.", difficulty="easy", category="numpy"),

  dict(id="case_066", buggy_code="x = np.arange(0, 10, 2)", task="Create an array of values from 0 to 10 (exclusive) in steps of 0.5", libs=["numpy"], error_type="wrong_arange_step", error_location="line 1: step=2 instead of step=0.5", clarification_query="What step value gives increments of 0.5?", clarification_options=["0.5","2","0.1","1"], expected_user_response="0.5", corrected_code="x = np.arange(0, 10, 0.5)", explanation="arange(0, 10, 2) produces [0, 2, 4, 6, 8]. A step of 0.5 produces [0, 0.5, 1.0, ..., 9.5].", difficulty="easy", category="numpy"),

  dict(id="case_067", buggy_code="result = np.dot(A, B.T)", task="Compute the dot product of matrix A and matrix B (no transpose needed)", libs=["numpy"], error_type="wrong_dot_product", error_location="line 1: B.T transposes B unnecessarily", clarification_query="If A is shape (m, k) and B is shape (k, n), how should the dot product be computed?", clarification_options=["np.dot(A, B)","np.dot(A, B.T)","np.dot(A.T, B)","np.dot(B, A)"], expected_user_response="np.dot(A, B)", corrected_code="result = np.dot(A, B)", explanation="np.dot(A, B) computes the matrix product when A is (m,k) and B is (k,n). Transposing B would require shape (n,k) instead.", difficulty="medium", category="numpy"),

  dict(id="case_068", buggy_code="stacked = np.vstack([arr1, arr2])", task="Stack two 1D arrays side by side as columns to form a 2D matrix", libs=["numpy"], error_type="wrong_vstack_vs_hstack", error_location="line 1: vstack stacks as rows, not columns", clarification_query="Which function stacks 1D arrays as columns of a 2D matrix?", clarification_options=["np.column_stack","np.vstack","np.hstack","np.stack(axis=0)"], expected_user_response="np.column_stack", corrected_code="stacked = np.column_stack([arr1, arr2])", explanation="vstack treats each 1D array as a row. column_stack treats them as columns, producing shape (n, 2).", difficulty="medium", category="numpy"),

  dict(id="case_069", buggy_code="result = np.concatenate([arr1, arr2], axis=1)", task="Concatenate two 2D arrays along the row axis (add more rows)", libs=["numpy"], error_type="wrong_concatenate_axis", error_location="line 1: axis=1 concatenates along columns", clarification_query="Which axis concatenates along rows (stacks vertically)?", clarification_options=["axis=0","axis=1","axis=-1","axis=2"], expected_user_response="axis=0", corrected_code="result = np.concatenate([arr1, arr2], axis=0)", explanation="axis=1 extends columns (horizontal). axis=0 extends rows (vertical), which is the requested operation.", difficulty="easy", category="numpy"),

  dict(id="case_070", buggy_code="result = np.where(arr > 0, arr, -arr)", task="Return the absolute value of each element (positive stays, negative becomes positive)", libs=["numpy"], error_type="wrong_where_condition", error_location="correct — validates np.where as abs pattern", clarification_query="Does np.where(arr>0, arr, -arr) correctly compute absolute value?", clarification_options=["yes, this is equivalent to np.abs(arr)","no, use np.abs(arr) only","no, the condition should be arr >= 0","no, -arr doesn't negate correctly"], expected_user_response="yes, this is equivalent to np.abs(arr)", corrected_code="result = np.where(arr > 0, arr, -arr)", explanation="np.where(arr>0, arr, -arr) returns arr when positive and -arr (which is positive) when negative, equivalent to np.abs.", difficulty="medium", category="numpy"),

  dict(id="case_071", buggy_code="any_positive = np.any(arr > 0, axis=1)\nall_positive = np.all(arr > 0, axis=0)", task="Check if any element per row is positive, and if all elements per column are positive", libs=["numpy"], error_type="wrong_any_all_axis", error_location="lines 1-2: axes are correct", clarification_query="For a 2D array, which axis checks across columns to get one result per row?", clarification_options=["axis=1","axis=0","axis=-1","no axis"], expected_user_response="axis=1", corrected_code="any_positive = np.any(arr > 0, axis=1)\nall_positive = np.all(arr > 0, axis=0)", explanation="axis=1 reduces columns → one result per row. axis=0 reduces rows → one result per column. Both usages here are correct.", difficulty="medium", category="numpy"),

  dict(id="case_072", buggy_code="cs = np.cumsum(matrix, axis=0)", task="Compute the cumulative sum across columns (within each row)", libs=["numpy"], error_type="wrong_cumsum_axis", error_location="line 1: axis=0 accumulates down rows", clarification_query="Which axis makes cumsum accumulate across columns (left to right) within each row?", clarification_options=["axis=1","axis=0","axis=-2","no axis"], expected_user_response="axis=1", corrected_code="cs = np.cumsum(matrix, axis=1)", explanation="axis=0 accumulates along the row direction (top to bottom). axis=1 accumulates along columns (left to right within a row).", difficulty="medium", category="numpy"),

  dict(id="case_073", buggy_code="d = np.diff(series, axis=1)", task="Compute first-order differences along the row axis (between consecutive rows)", libs=["numpy"], error_type="wrong_diff_axis", error_location="line 1: axis=1 diffs along columns", clarification_query="Which axis computes differences between consecutive rows?", clarification_options=["axis=0","axis=1","axis=-1","axis=2"], expected_user_response="axis=0", corrected_code="d = np.diff(series, axis=0)", explanation="axis=1 differences adjacent columns within a row. axis=0 differences adjacent rows (consecutive time steps).", difficulty="medium", category="numpy"),

  dict(id="case_074", buggy_code="corr = np.corrcoef(x, y)[0][0]", task="Extract the correlation coefficient between arrays x and y", libs=["numpy"], error_type="wrong_corrcoef", error_location="line 1: [0][0] extracts variance of x, not correlation", clarification_query="In the 2x2 correlation matrix returned by np.corrcoef(x,y), where is the correlation between x and y?", clarification_options=["[0][1] or [1][0]","[0][0]","[1][1]","[0][0] and [1][1]"], expected_user_response="[0][1] or [1][0]", corrected_code="corr = np.corrcoef(x, y)[0][1]", explanation="np.corrcoef returns [[var(x), corr(x,y)],[corr(y,x), var(y)]]. The cross-correlation is at [0,1] or [1,0], not [0,0].", difficulty="hard", category="numpy"),

  dict(id="case_075", buggy_code="coeffs = np.polyfit(x, y, deg=1)\nfit = np.polyval(coeffs, x)", task="Fit a quadratic (degree 2) polynomial to data points", libs=["numpy"], error_type="wrong_polyfit_deg", error_location="line 1: deg=1 fits a line, not a quadratic", clarification_query="What degree argument fits a quadratic polynomial?", clarification_options=["deg=2","deg=1","deg=3","deg=0"], expected_user_response="deg=2", corrected_code="coeffs = np.polyfit(x, y, deg=2)\nfit = np.polyval(coeffs, x)", explanation="deg=1 fits a line (y = ax + b). deg=2 fits a quadratic (y = ax² + bx + c).", difficulty="easy", category="numpy"),

  dict(id="case_076", buggy_code="norm = np.linalg.norm(matrix, axis=0)", task="Compute the L2 norm of each row vector in a 2D matrix", libs=["numpy"], error_type="wrong_linalg_norm_axis", error_location="line 1: axis=0 computes column norms, not row norms", clarification_query="Which axis computes the norm of each row?", clarification_options=["axis=1","axis=0","axis=-2","no axis"], expected_user_response="axis=1", corrected_code="norm = np.linalg.norm(matrix, axis=1)", explanation="axis=0 treats each column as a vector. axis=1 treats each row as a vector and returns one norm per row.", difficulty="medium", category="numpy"),

  dict(id="case_077", buggy_code="tiled = np.tile(arr, (2, 1))", task="Tile a 1D array 3 times along the column axis to form a 2D matrix with 3 copies", libs=["numpy"], error_type="wrong_tile_reps", error_location="line 1: (2,1) tiles 2 rows of 1 copy; should be (1,3)", clarification_query="What reps tuple tiles a 1D array 3 times along columns?", clarification_options=["(1, 3)","(2, 1)","(3,)","(3, 1)"], expected_user_response="(1, 3)", corrected_code="tiled = np.tile(arr, (1, 3))", explanation="np.tile(arr, (2,1)) makes 2 row copies of 1 column-copy. (1, 3) keeps 1 row and repeats 3 times along columns.", difficulty="medium", category="numpy"),

  dict(id="case_078", buggy_code="repeated = np.repeat(arr, 3, axis=0)", task="Repeat each element of a 1D array 3 times (element-wise repeat)", libs=["numpy"], error_type="wrong_repeat_axis", error_location="line 1: axis=0 on 1D is fine but verifies understanding", clarification_query="For a 1D array, does np.repeat(arr, 3) repeat each element 3 times?", clarification_options=["yes","no, it repeats the whole array 3 times","no, use np.tile","no, axis must be specified"], expected_user_response="yes", corrected_code="repeated = np.repeat(arr, 3)", explanation="np.repeat(arr, 3) with a 1D array repeats each element 3 times in place. No axis specification needed for 1D.", difficulty="easy", category="numpy"),

  dict(id="case_079", buggy_code="unique_vals, counts = np.unique(arr, return_counts=False)", task="Get unique values and their counts from an array", libs=["numpy"], error_type="wrong_unique_return_counts", error_location="line 1: return_counts=False omits counts", clarification_query="What argument makes np.unique also return the count of each unique value?", clarification_options=["return_counts=True","return_counts=False","counts=True","with_counts=True"], expected_user_response="return_counts=True", corrected_code="unique_vals, counts = np.unique(arr, return_counts=True)", explanation="return_counts=False (default) returns only unique values. return_counts=True adds a second array of occurrence counts.", difficulty="easy", category="numpy"),

  dict(id="case_080", buggy_code="idx = np.searchsorted(sorted_arr, value, side='right')", task="Find the leftmost insertion point for value in a sorted array", libs=["numpy"], error_type="wrong_searchsorted", error_location="line 1: side='right' gives insertion after existing matches", clarification_query="Which side argument returns the leftmost insertion index?", clarification_options=["side='left'","side='right'","side='first'","side='start'"], expected_user_response="side='left'", corrected_code="idx = np.searchsorted(sorted_arr, value, side='left')", explanation="side='right' inserts after any existing equal values. side='left' returns the leftmost valid position.", difficulty="medium", category="numpy"),

  dict(id="case_081", buggy_code="sorted_indices = np.argsort(arr)[::-1]\ntop3 = arr[sorted_indices[:3]]", task="Get the 3 smallest values using argsort", libs=["numpy"], error_type="wrong_argsort_order", error_location="line 1: [::-1] reverses to descending; omit for ascending", clarification_query="To get indices of smallest values first, how should argsort be called?", clarification_options=["np.argsort(arr) without reversal","np.argsort(arr)[::-1]","np.argsort(-arr)","np.argsort(arr, order='descending')"], expected_user_response="np.argsort(arr) without reversal", corrected_code="sorted_indices = np.argsort(arr)\ntop3 = arr[sorted_indices[:3]]", explanation="argsort returns indices in ascending order by default. Reversing with [::-1] gives largest first. For smallest 3, no reversal needed.", difficulty="medium", category="numpy"),

  dict(id="case_082", buggy_code="p90 = np.percentile(data, 0.9)", task="Compute the 90th percentile of the data", libs=["numpy"], error_type="wrong_percentile", error_location="line 1: argument 0.9 instead of 90", clarification_query="np.percentile takes a percentage value (0-100) or a fraction (0-1)?", clarification_options=["0-100 integer/float (e.g. 90)","0-1 fraction (e.g. 0.9)","either format works","depends on NumPy version"], expected_user_response="0-100 integer/float (e.g. 90)", corrected_code="p90 = np.percentile(data, 90)", explanation="np.percentile expects q in the range [0, 100], not [0, 1]. 0.9 would return near the minimum value.", difficulty="easy", category="numpy"),

  dict(id="case_083", buggy_code="counts, edges = np.histogram(data, bins=5)\nbucket_centers = (edges[:-1] + edges[1:]) / 2", task="Compute histogram with 20 bins and find bucket centers", libs=["numpy"], error_type="wrong_histogram_bins", error_location="line 1: bins=5 instead of bins=20", clarification_query="What bins value produces 20 buckets?", clarification_options=["20","5","10","'auto'"], expected_user_response="20", corrected_code="counts, edges = np.histogram(data, bins=20)\nbucket_centers = (edges[:-1] + edges[1:]) / 2", explanation="bins=5 produces 5 buckets. The task requires 20 buckets.", difficulty="easy", category="numpy"),

  dict(id="case_084", buggy_code="z = np.zeros((3, 4))\nz[1, 1] = 1", task="Create a 4x4 identity matrix", libs=["numpy"], error_type="wrong_zeros_shape", error_location="line 1: zeros used with wrong shape instead of np.eye", clarification_query="Which NumPy function directly creates an identity matrix?", clarification_options=["np.eye(4)","np.zeros((4,4)) with manual diagonal","np.identity(4)","both np.eye(4) and np.identity(4)"], expected_user_response="both np.eye(4) and np.identity(4)", corrected_code="z = np.eye(4)", explanation="Manually setting elements in a zeros array is tedious and error-prone. np.eye(4) or np.identity(4) directly creates the identity.", difficulty="easy", category="numpy"),

  dict(id="case_085", buggy_code="rng = np.random.default_rng(42)\nsamples = rng.normal(0, 1, size=1000)\nnp.random.seed(42)\nsamples2 = np.random.normal(0, 1, 1000)", task="Use only the new Generator API (default_rng) consistently; don't mix with legacy np.random.seed", libs=["numpy"], error_type="wrong_random_seed", error_location="lines 3-4: mixing legacy np.random.seed with new Generator API", clarification_query="Should np.random.seed() be mixed with the default_rng() Generator API?", clarification_options=["no, use rng = np.random.default_rng(seed) for everything","yes, they work together","yes, seed sets global state for both","no, use np.random.seed only"], expected_user_response="no, use rng = np.random.default_rng(seed) for everything", corrected_code="rng = np.random.default_rng(42)\nsamples = rng.normal(0, 1, size=1000)\nsamples2 = rng.normal(0, 1, size=1000)", explanation="np.random.seed() controls the legacy global RNG, separate from Generator instances. Mixing them leads to non-reproducible results.", difficulty="hard", category="numpy"),

  dict(id="case_086", buggy_code="outer = np.outer(v1, v2)", task="Compute the inner (dot) product of two 1D vectors", libs=["numpy"], error_type="wrong_outer_product", error_location="line 1: np.outer computes outer product, not inner/dot product", clarification_query="Which function computes the scalar dot product of two 1D vectors?", clarification_options=["np.dot(v1, v2)","np.outer(v1, v2)","np.inner(v1, v2) — same as dot for 1D","both np.dot and np.inner"], expected_user_response="both np.dot and np.inner", corrected_code="outer = np.dot(v1, v2)", explanation="np.outer(v1, v2) produces a 2D rank-1 matrix. np.dot(v1, v2) or np.inner(v1, v2) gives the scalar dot product.", difficulty="medium", category="numpy"),

  dict(id="case_087", buggy_code="interp_val = np.interp(x_new, xp, fp[::-1])", task="Interpolate y values at new x points using xp (ascending) and fp", libs=["numpy"], error_type="wrong_interp_xp", error_location="line 1: fp is reversed unnecessarily", clarification_query="Must xp in np.interp be in ascending order, and should fp be reversed to match?", clarification_options=["xp must be ascending; fp must match xp order (no reversal)","both must be ascending","both can be in any order","fp must always be reversed"], expected_user_response="xp must be ascending; fp must match xp order (no reversal)", corrected_code="interp_val = np.interp(x_new, xp, fp)", explanation="np.interp requires xp in ascending order. fp must correspond element-wise to xp — reversing fp would pair each x with the wrong y.", difficulty="hard", category="numpy"),

  dict(id="case_088", buggy_code="gradient = np.gradient(y, dx=0.5)\nprint(gradient.shape)", task="Compute numerical gradient of y sampled at uniform spacing of 1.0", libs=["numpy"], error_type="wrong_gradient", error_location="line 1: dx=0.5 instead of dx=1.0", clarification_query="What spacing value should be used when y is sampled at integer intervals (x=0,1,2,...)?", clarification_options=["1.0 (or just np.gradient(y))","0.5","2.0","depends on length of y"], expected_user_response="1.0 (or just np.gradient(y))", corrected_code="gradient = np.gradient(y)\nprint(gradient.shape)", explanation="np.gradient defaults to spacing=1 which matches integer-sampled data. dx=0.5 would halve the spacing, doubling gradient values.", difficulty="medium", category="numpy"),

  dict(id="case_089", buggy_code="cross = np.cross(v1, v2)\nprint(cross)", task="Compute the dot product of v1 and v2, not the cross product", libs=["numpy"], error_type="wrong_cross_product", error_location="line 1: np.cross computes cross product", clarification_query="Which function computes the dot (scalar) product of two vectors?", clarification_options=["np.dot(v1, v2)","np.cross(v1, v2)","np.multiply(v1, v2)","np.inner(v1, v2) for 1D"], expected_user_response="np.dot(v1, v2)", corrected_code="cross = np.dot(v1, v2)\nprint(cross)", explanation="np.cross returns the cross product vector. np.dot returns the scalar dot product.", difficulty="easy", category="numpy"),

  dict(id="case_090", buggy_code="result = np.inner(A, B)", task="Compute the standard matrix multiplication of 2D arrays A (m,k) and B (k,n)", libs=["numpy"], error_type="wrong_inner_product", error_location="line 1: np.inner for 2D does not compute standard matmul", clarification_query="For 2D arrays, which function computes standard matrix multiplication?", clarification_options=["np.matmul(A, B) or A @ B","np.inner(A, B)","np.dot(A, B)","both np.matmul and np.dot"], expected_user_response="both np.matmul and np.dot", corrected_code="result = np.matmul(A, B)", explanation="np.inner for 2D arrays computes sum of products of last axes, not standard matmul. np.matmul or @ operator is correct.", difficulty="hard", category="numpy"),

  dict(id="case_091", buggy_code="ma = np.ma.masked_where(arr < 0, arr)\nresult = ma.mean()", task="Mask values above 100 (not below 0) before computing the mean", libs=["numpy"], error_type="wrong_masked_fill", error_location="line 1: masking condition is < 0 instead of > 100", clarification_query="What condition should be used to mask values above 100?", clarification_options=["arr > 100","arr < 0","arr >= 100","arr != 100"], expected_user_response="arr > 100", corrected_code="ma = np.ma.masked_where(arr > 100, arr)\nresult = ma.mean()", explanation="masked_where(condition, arr) masks elements where condition is True. To exclude values above 100, use arr > 100.", difficulty="easy", category="numpy"),

  dict(id="case_092", buggy_code="eye = np.eye(5, dtype=int)\nprint(eye.shape)", task="Create a 3x3 identity matrix with integer dtype", libs=["numpy"], error_type="wrong_eye_size", error_location="line 1: np.eye(5) creates 5x5, not 3x3", clarification_query="What first argument to np.eye creates a 3x3 matrix?", clarification_options=["3","5","(3,3)","np.eye(N=3)"], expected_user_response="3", corrected_code="eye = np.eye(3, dtype=int)\nprint(eye.shape)", explanation="np.eye(5) creates a 5x5 identity matrix. np.eye(3) creates the required 3x3 matrix.", difficulty="easy", category="numpy"),

  dict(id="case_093", buggy_code="result = np.einsum('ij,jk->ik', A, B.T)", task="Use einsum to perform standard matrix multiplication A @ B", libs=["numpy"], error_type="wrong_einsum", error_location="line 1: B.T transposes B, changing the operation", clarification_query="What einsum string performs A @ B (standard matmul) without transposing B?", clarification_options=["'ij,jk->ik'","'ij,kj->ik'","'ij,jk->ki'","'ij,jk->ij'"], expected_user_response="'ij,jk->ik'", corrected_code="result = np.einsum('ij,jk->ik', A, B)", explanation="'ij,jk->ik' sums over j (the shared dimension) producing (i,k). Passing B.T changes the semantics to 'ij,kj->ik'.", difficulty="hard", category="numpy"),

  dict(id="case_094", buggy_code="idx = np.argsort(scores)[-1]\nbest = names[idx]", task="Find the name corresponding to the top 3 highest scores", libs=["numpy"], error_type="wrong_argmax", error_location="line 1: [-1] retrieves only the single best, not top 3", clarification_query="How do you get the indices of the top 3 highest scores?", clarification_options=["np.argsort(scores)[-3:]","np.argsort(scores)[-1]","np.argmax(scores)","np.argsort(-scores)[:3]"], expected_user_response="np.argsort(scores)[-3:]", corrected_code="top3_idx = np.argsort(scores)[-3:]\nbest = names[top3_idx]", explanation="[-1] gives only the highest index. [-3:] gives the last 3 (highest 3 values when sorted ascending).", difficulty="medium", category="numpy"),

  dict(id="case_095", buggy_code="flat_idx = np.ravel_multi_index((2, 3), (5, 5))\nprint(flat_idx)", task="Convert 2D index (row=3, col=2) in a 5x5 array to a flat index", libs=["numpy"], error_type="wrong_reshape_order", error_location="line 1: row and col order swapped in the tuple", clarification_query="In np.ravel_multi_index((row, col), shape), what is the correct order?", clarification_options=["(row, col) — row first","(col, row) — col first","(col, row) for C order","order doesn't matter"], expected_user_response="(row, col) — row first", corrected_code="flat_idx = np.ravel_multi_index((3, 2), (5, 5))\nprint(flat_idx)", explanation="ravel_multi_index takes (row_index, col_index). The task specifies row=3, col=2 but the code has them swapped.", difficulty="hard", category="numpy"),
]

# ── MATPLOTLIB (cases 096-123, 28 cases) ──────────────────────────────────
CASES += [
  dict(id="case_096", buggy_code="plt.xlabel('Values')\nplt.ylabel('Time (s)')", task="Label the x-axis as 'Time (s)' and y-axis as 'Values'", libs=["matplotlib"], error_type="wrong_xlabel_ylabel_swap", error_location="lines 1-2: xlabel and ylabel are swapped", clarification_query="Which axis should be labeled 'Time (s)'?", clarification_options=["x-axis (horizontal)","y-axis (vertical)","both","neither"], expected_user_response="x-axis (horizontal)", corrected_code="plt.xlabel('Time (s)')\nplt.ylabel('Values')", explanation="xlabel labels the horizontal axis, ylabel the vertical. They are swapped in the buggy code.", difficulty="easy", category="matplotlib"),

  dict(id="case_097", buggy_code="plt.plot(x, y, label='Training loss')\nplt.plot(x, val_y, label='Validation loss')\nplt.show()", task="Plot two lines and display a legend", libs=["matplotlib"], error_type="missing_legend", error_location="line 3: plt.legend() not called before plt.show()", clarification_query="What function renders the legend on the plot?", clarification_options=["plt.legend()","plt.label()","plt.add_legend()","labels appear automatically"], expected_user_response="plt.legend()", corrected_code="plt.plot(x, y, label='Training loss')\nplt.plot(x, val_y, label='Validation loss')\nplt.legend()\nplt.show()", explanation="Labels passed to plot() are not displayed unless plt.legend() is called.", difficulty="easy", category="matplotlib"),

  dict(id="case_098", buggy_code="plt.imshow(heatmap, cmap='viridis')\nplt.show()", task="Display a heatmap and add a colorbar to show the value scale", libs=["matplotlib"], error_type="missing_colorbar", error_location="line 2: plt.colorbar() not called", clarification_query="What function adds a colorbar to a matplotlib figure?", clarification_options=["plt.colorbar()","plt.add_colorbar()","plt.cbar()","plt.legend(kind='color')"], expected_user_response="plt.colorbar()", corrected_code="plt.imshow(heatmap, cmap='viridis')\nplt.colorbar()\nplt.show()", explanation="imshow renders the color-mapped image but the color scale is not shown without plt.colorbar().", difficulty="easy", category="matplotlib"),

  dict(id="case_099", buggy_code="fig, axes = plt.subplots(2, 2)\naxes[1][2].plot(x, y)", task="Plot on the bottom-right subplot of a 2x2 grid", libs=["matplotlib"], error_type="wrong_subplot_index", error_location="line 2: axes[1][2] is out of bounds; should be axes[1][1]", clarification_query="In a 2x2 subplot grid, what index accesses the bottom-right subplot?", clarification_options=["axes[1][1]","axes[1][2]","axes[2][1]","axes[2][2]"], expected_user_response="axes[1][1]", corrected_code="fig, axes = plt.subplots(2, 2)\naxes[1][1].plot(x, y)", explanation="A 2x2 grid has indices 0 and 1 in each dimension. axes[1][2] raises IndexError; bottom-right is axes[1][1].", difficulty="easy", category="matplotlib"),

  dict(id="case_100", buggy_code="fig = plt.figure(figsize=(4, 8))", task="Create a figure with width 8 inches and height 4 inches (landscape)", libs=["matplotlib"], error_type="wrong_figure_size", error_location="line 1: (4, 8) is portrait (width=4, height=8)", clarification_query="figsize=(width, height) — what values give an 8-wide, 4-tall figure?", clarification_options=["(8, 4)","(4, 8)","(8, 8)","(4, 4)"], expected_user_response="(8, 4)", corrected_code="fig = plt.figure(figsize=(8, 4))", explanation="figsize=(width, height). (4, 8) makes a tall portrait figure. (8, 4) makes the requested wide landscape figure.", difficulty="easy", category="matplotlib"),

  dict(id="case_101", buggy_code="plt.plot(x, y)\nplt.show()", task="Plot y vs x and add a descriptive title 'Sales Over Time'", libs=["matplotlib"], error_type="missing_title", error_location="line 2: plt.title() not called", clarification_query="Which function sets the plot title?", clarification_options=["plt.title('Sales Over Time')","plt.label('Sales Over Time')","plt.header('Sales Over Time')","plt.name('Sales Over Time')"], expected_user_response="plt.title('Sales Over Time')", corrected_code="plt.plot(x, y)\nplt.title('Sales Over Time')\nplt.show()", explanation="plt.title() sets the text displayed above the plot area.", difficulty="easy", category="matplotlib"),

  dict(id="case_102", buggy_code="plt.plot(x, y, marker='x', linestyle='solid')", task="Plot a scatter-like line using circle markers with dashed line style", libs=["matplotlib"], error_type="wrong_marker_style", error_location="line 1: marker='x' should be 'o'; linestyle='solid' should be 'dashed'", clarification_query="Which marker and linestyle produce circle markers with a dashed line?", clarification_options=["marker='o', linestyle='dashed'","marker='x', linestyle='solid'","marker='o', linestyle='dotted'","marker='circle', linestyle='--'"], expected_user_response="marker='o', linestyle='dashed'", corrected_code="plt.plot(x, y, marker='o', linestyle='dashed')", explanation="'x' is an X marker; 'o' gives circles. 'solid' is the default line; 'dashed' gives a dashed line.", difficulty="easy", category="matplotlib"),

  dict(id="case_103", buggy_code="plt.bar(categories, values, width=0.9)", task="Plot a bar chart with narrow bars (width=0.4) to show gaps between bars", libs=["matplotlib"], error_type="wrong_bar_width", error_location="line 1: width=0.9 makes bars very wide with minimal gaps", clarification_query="What width value produces narrow bars with visible gaps?", clarification_options=["0.4","0.9","1.0","1.5"], expected_user_response="0.4", corrected_code="plt.bar(categories, values, width=0.4)", explanation="width=0.9 fills 90% of each category slot, leaving tiny gaps. width=0.4 creates clearly visible gaps.", difficulty="easy", category="matplotlib"),

  dict(id="case_104", buggy_code="plt.scatter(x, y, s=500)", task="Create a scatter plot with small point size (s=20) for large datasets", libs=["matplotlib"], error_type="wrong_scatter_size", error_location="line 1: s=500 creates oversized points", clarification_query="What marker size is appropriate for a large, dense scatter plot?", clarification_options=["s=20","s=500","s=200","s=1000"], expected_user_response="s=20", corrected_code="plt.scatter(x, y, s=20)", explanation="s=500 creates very large markers that overlap heavily on dense data. s=20 gives small, distinguishable points.", difficulty="easy", category="matplotlib"),

  dict(id="case_105", buggy_code="plt.hist(data, bins=3)", task="Plot a histogram of data with 30 bins for fine-grained distribution", libs=["matplotlib"], error_type="wrong_hist_bins", error_location="line 1: bins=3 gives too few bins for fine resolution", clarification_query="What bin count gives fine-grained resolution for the histogram?", clarification_options=["30","3","10","5"], expected_user_response="30", corrected_code="plt.hist(data, bins=30)", explanation="bins=3 produces only 3 bars, hiding the distribution shape. bins=30 gives much finer resolution.", difficulty="easy", category="matplotlib"),

  dict(id="case_106", buggy_code="fig, axes = plt.subplots(1, 3)\nfor ax in axes:\n    ax.plot(x, y)\nplt.show()", task="Display subplots without overlapping labels using tight layout", libs=["matplotlib"], error_type="missing_tight_layout", error_location="line 4: plt.tight_layout() not called before plt.show()", clarification_query="Which function prevents subplots from overlapping?", clarification_options=["plt.tight_layout()","plt.adjust()","plt.spacing()","plt.subplots_adjust(hspace=0.5)"], expected_user_response="plt.tight_layout()", corrected_code="fig, axes = plt.subplots(1, 3)\nfor ax in axes:\n    ax.plot(x, y)\nplt.tight_layout()\nplt.show()", explanation="Without tight_layout(), subplots may overlap at their labels and titles.", difficulty="easy", category="matplotlib"),

  dict(id="case_107", buggy_code="plt.xticks(rotation=0)", task="Rotate x-axis tick labels 45 degrees for readability", libs=["matplotlib"], error_type="wrong_tick_rotation", error_location="line 1: rotation=0 means no rotation", clarification_query="What rotation angle in degrees makes x-axis labels diagonal for readability?", clarification_options=["45","0","90","180"], expected_user_response="45", corrected_code="plt.xticks(rotation=45)", explanation="rotation=0 keeps labels horizontal. rotation=45 tilts them diagonally to prevent overlapping long category labels.", difficulty="easy", category="matplotlib"),

  dict(id="case_108", buggy_code="plt.plot(x, y, alpha=0.0)", task="Plot a semi-transparent line with alpha=0.5", libs=["matplotlib"], error_type="wrong_alpha_value", error_location="line 1: alpha=0.0 makes the line fully transparent (invisible)", clarification_query="What alpha value gives 50% transparency?", clarification_options=["0.5","0.0","1.0","0.1"], expected_user_response="0.5", corrected_code="plt.plot(x, y, alpha=0.5)", explanation="alpha=0.0 is fully transparent (invisible). alpha=0.5 gives 50% transparency. alpha=1.0 is fully opaque.", difficulty="easy", category="matplotlib"),

  dict(id="case_109", buggy_code="plt.grid(axis='x')", task="Add a grid on both axes (x and y)", libs=["matplotlib"], error_type="wrong_grid_axis", error_location="line 1: axis='x' adds grid only on x-axis", clarification_query="What axis argument adds gridlines on both axes?", clarification_options=["axis='both'","axis='x'","axis='y'","axis='all'"], expected_user_response="axis='both'", corrected_code="plt.grid(axis='both')", explanation="axis='x' draws vertical gridlines only. axis='both' draws gridlines for both x and y axes.", difficulty="easy", category="matplotlib"),

  dict(id="case_110", buggy_code="fig, ax1 = plt.subplots()\nax2 = ax1.twinx()\nax1.plot(x, y1, 'b')\nax2.plot(x, y2, 'r')\nax1.set_ylabel('Temperature', color='b')\nax1.set_ylabel('Pressure', color='r')", task="Create dual y-axes: Temperature on left, Pressure on right", libs=["matplotlib"], error_type="wrong_twin_axis", error_location="line 6: ax1.set_ylabel used again instead of ax2.set_ylabel", clarification_query="Which axis object should label the right y-axis (Pressure)?", clarification_options=["ax2","ax1","both ax1 and ax2","plt.ylabel"], expected_user_response="ax2", corrected_code="fig, ax1 = plt.subplots()\nax2 = ax1.twinx()\nax1.plot(x, y1, 'b')\nax2.plot(x, y2, 'r')\nax1.set_ylabel('Temperature', color='b')\nax2.set_ylabel('Pressure', color='r')", explanation="ax1.twinx() creates ax2 sharing the x-axis with a separate right y-axis. ax2.set_ylabel sets the right label.", difficulty="medium", category="matplotlib"),

  dict(id="case_111", buggy_code="plt.fill_between(x, y1, y2, where=(y1 < y2))", task="Shade the region where y1 is greater than y2", libs=["matplotlib"], error_type="wrong_fill_between", error_location="line 1: condition is y1 < y2, should be y1 > y2", clarification_query="What where condition shades regions where y1 exceeds y2?", clarification_options=["y1 > y2","y1 < y2","y1 == y2","y1 >= y2"], expected_user_response="y1 > y2", corrected_code="plt.fill_between(x, y1, y2, where=(y1 > y2))", explanation="where=(y1 < y2) shades where y2 is on top. where=(y1 > y2) shades where y1 is on top.", difficulty="easy", category="matplotlib"),

  dict(id="case_112", buggy_code="plt.errorbar(x, y, yerr=errors, fmt='none')", task="Plot data points connected by a line with error bars", libs=["matplotlib"], error_type="wrong_errorbar_fmt", error_location="line 1: fmt='none' suppresses the data line/markers", clarification_query="What fmt value draws data points connected by a line with error bars?", clarification_options=["'-o'","'none'","'o'","'--'"], expected_user_response="'-o'", corrected_code="plt.errorbar(x, y, yerr=errors, fmt='-o')", explanation="fmt='none' suppresses the data line and markers, showing only error bars. '-o' gives a line with circular markers.", difficulty="medium", category="matplotlib"),

  dict(id="case_113", buggy_code="plt.pie(values, labels=None)", task="Create a pie chart with category labels", libs=["matplotlib"], error_type="wrong_pie_labels", error_location="line 1: labels=None suppresses category labels", clarification_query="How should category names be passed to display pie chart labels?", clarification_options=["labels=category_list","labels=None","labels='auto'","labels=True"], expected_user_response="labels=category_list", corrected_code="plt.pie(values, labels=categories)", explanation="labels=None (default) shows no labels on pie slices. Pass the list of category names to display them.", difficulty="easy", category="matplotlib"),

  dict(id="case_114", buggy_code="plt.contour(X, Y, Z, levels=3)", task="Plot contour lines with 20 levels for smooth visualization", libs=["matplotlib"], error_type="wrong_contour_levels", error_location="line 1: levels=3 produces only 3 contour lines", clarification_query="What levels value produces 20 contour lines?", clarification_options=["20","3","10","5"], expected_user_response="20", corrected_code="plt.contour(X, Y, Z, levels=20)", explanation="levels=3 draws only 3 contour lines, giving a coarse view. levels=20 provides smooth, detailed contours.", difficulty="easy", category="matplotlib"),

  dict(id="case_115", buggy_code="plt.savefig('output.png')", task="Save the figure at high resolution (300 DPI) for publication", libs=["matplotlib"], error_type="missing_savefig_dpi", error_location="line 1: dpi not specified, defaults to 100", clarification_query="Which parameter sets the resolution when saving a figure?", clarification_options=["dpi=300","resolution=300","quality=300","ppi=300"], expected_user_response="dpi=300", corrected_code="plt.savefig('output.png', dpi=300)", explanation="savefig defaults to 100 DPI. For print-quality figures, dpi=300 or higher is standard.", difficulty="easy", category="matplotlib"),

  dict(id="case_116", buggy_code="plt.imshow(img, cmap='gray')", task="Display a color image array (RGB) — do not apply a colormap", libs=["matplotlib"], error_type="wrong_imshow_cmap", error_location="line 1: cmap='gray' converts color image to grayscale", clarification_query="How should a 3-channel RGB image be displayed without colormap distortion?", clarification_options=["plt.imshow(img) with no cmap","plt.imshow(img, cmap='gray')","plt.imshow(img, cmap='rgb')","plt.imshow(img, cmap=None, mode='RGB')"], expected_user_response="plt.imshow(img) with no cmap", corrected_code="plt.imshow(img)", explanation="cmap='gray' maps single scalar values to gray shades. For RGB arrays, omit cmap so the 3 channels map to R, G, B.", difficulty="medium", category="matplotlib"),

  dict(id="case_117", buggy_code="fig, ax = plt.subplots()\nax.boxplot([group1, group2, group3], positions=[1, 2, 3])\nax.boxplot([group4, group5], positions=[4, 5])", task="Plot all 5 groups in a single boxplot call", libs=["matplotlib"], error_type="wrong_boxplot_positions", error_location="lines 2-3: two separate boxplot calls on same axes overlap default styling", clarification_query="How should all 5 groups be combined into one boxplot call?", clarification_options=["ax.boxplot([g1,g2,g3,g4,g5], positions=[1,2,3,4,5])","call boxplot twice, once per set","use ax.violinplot instead","use positions='auto'"], expected_user_response="ax.boxplot([g1,g2,g3,g4,g5], positions=[1,2,3,4,5])", corrected_code="fig, ax = plt.subplots()\nax.boxplot([group1, group2, group3, group4, group5], positions=[1, 2, 3, 4, 5])", explanation="Calling boxplot twice on the same axes creates overlapping whisker caps. One call with all groups is the correct approach.", difficulty="medium", category="matplotlib"),

  dict(id="case_118", buggy_code="plt.plot(x, y)\nplt.xlim(10, 0)", task="Set x-axis limits from 0 to 10 (left to right)", libs=["matplotlib"], error_type="wrong_xlim_ylim", error_location="line 2: xlim(10, 0) reverses the x-axis", clarification_query="What xlim call sets x from 0 on the left to 10 on the right?", clarification_options=["xlim(0, 10)","xlim(10, 0)","xlim(min=0, max=10)","xlim([0, 10])"], expected_user_response="xlim(0, 10)", corrected_code="plt.plot(x, y)\nplt.xlim(0, 10)", explanation="xlim(10, 0) makes the x-axis decrease from left to right. xlim(0, 10) sets the conventional left-to-right direction.", difficulty="easy", category="matplotlib"),

  dict(id="case_119", buggy_code="plt.plot(x, y)\n# forgot plt.show()", task="Display the plot after drawing the line", libs=["matplotlib"], error_type="missing_plt_show", error_location="line 2: plt.show() not called", clarification_query="What function is required to display a matplotlib plot in a script?", clarification_options=["plt.show()","plt.display()","plt.render()","plt.draw()"], expected_user_response="plt.show()", corrected_code="plt.plot(x, y)\nplt.show()", explanation="In scripts, plt.show() is required to render and open the plot window. Without it, the figure is created but not displayed.", difficulty="easy", category="matplotlib"),

  dict(id="case_120", buggy_code="plt.plot(x, y, linestyle='dotted')", task="Plot a line with a dash-dot style (alternating dashes and dots)", libs=["matplotlib"], error_type="wrong_linestyle", error_location="line 1: 'dotted' gives only dots, not dash-dot", clarification_query="Which linestyle string gives alternating dashes and dots?", clarification_options=["'dashdot'","'dotted'","'dashed'","'solid'"], expected_user_response="'dashdot'", corrected_code="plt.plot(x, y, linestyle='dashdot')", explanation="'dotted' uses only dots. 'dashdot' alternates between dashes and dots.", difficulty="easy", category="matplotlib"),

  dict(id="case_121", buggy_code="plt.plot(x, y, color='cyan')", task="Plot a line using the standard blue color", libs=["matplotlib"], error_type="wrong_color_map", error_location="line 1: 'cyan' is not the standard blue", clarification_query="Which color string gives the standard matplotlib blue?", clarification_options=["'blue' or 'b'","'cyan'","'navy'","'steelblue'"], expected_user_response="'blue' or 'b'", corrected_code="plt.plot(x, y, color='blue')", explanation="'cyan' is a bright blue-green color. The standard blue is specified as 'blue' or the shorthand 'b'.", difficulty="easy", category="matplotlib"),

  dict(id="case_122", buggy_code="fig, ax = plt.subplots()\nax.scatter(x, y)\nax2 = ax.twiny()\nax2.plot(x2, y2)", task="Add a second x-axis sharing the same y-axis (twin y, not twin x)", libs=["matplotlib"], error_type="wrong_twin_axis", error_location="line 3: twiny() shares x — should use twinx() to share y", clarification_query="Which method creates a second x-axis that shares the same y-axis?", clarification_options=["ax.twiny()","ax.twinx()","ax.twin('x')","ax.secondary_xaxis()"], expected_user_response="ax.twiny()", corrected_code="fig, ax = plt.subplots()\nax.scatter(x, y)\nax2 = ax.twiny()\nax2.plot(x2, y2)", explanation="twiny() creates a new x-axis at the top sharing the y-axis. twinx() creates a new y-axis on the right sharing the x-axis.", difficulty="medium", category="matplotlib"),

  dict(id="case_123", buggy_code="plt.savefig('chart.pdf')\nplt.show()", task="Show the plot before saving to avoid a blank file", libs=["matplotlib"], error_type="missing_plt_show", error_location="line 1: savefig before show can work but show() clears the figure first", clarification_query="Should savefig come before or after show() to avoid saving a blank figure?", clarification_options=["savefig before show()","savefig after show()","order doesn't matter","only use savefig, skip show()"], expected_user_response="savefig before show()", corrected_code="plt.savefig('chart.pdf')\nplt.show()", explanation="plt.show() clears the current figure. If savefig is called after show(), it saves an empty figure. Saving before show() is correct.", difficulty="medium", category="matplotlib"),
]

# ── MISC (cases 124-150, 27 cases) ────────────────────────────────────────
CASES += [
  dict(id="case_124", buggy_code="if status == 'Active':\n    print('enabled')", task="Check if status equals 'active' (lowercase), then print 'enabled'", libs=["python"], error_type="wrong_string_comparison", error_location="line 1: 'Active' differs in case from 'active'", clarification_query="Is string comparison in Python case-sensitive?", clarification_options=["yes, 'Active' != 'active'","no, comparison is case-insensitive","only for ASCII","depends on locale"], expected_user_response="yes, 'Active' != 'active'", corrected_code="if status == 'active':\n    print('enabled')", explanation="Python string comparison is case-sensitive. 'Active' does not equal 'active'.", difficulty="easy", category="misc"),

  dict(id="case_125", buggy_code="def compute_total(items):\n    total = sum(items)\n\nresult = compute_total([1,2,3])\nprint(result)", task="Define a function that returns the sum of items", libs=["python"], error_type="missing_return", error_location="line 2: function has no return statement", clarification_query="What statement is needed to output the computed total from the function?", clarification_options=["return total","print(total)","yield total","output total"], expected_user_response="return total", corrected_code="def compute_total(items):\n    total = sum(items)\n    return total\n\nresult = compute_total([1,2,3])\nprint(result)", explanation="Without a return statement, the function returns None. Adding 'return total' passes the value to the caller.", difficulty="easy", category="misc"),

  dict(id="case_126", buggy_code="for i in range(1, len(items)):\n    process(items[i])", task="Process every element in the list starting from index 0", libs=["python"], error_type="wrong_loop_range", error_location="line 1: range starts at 1, skipping index 0", clarification_query="What range arguments include index 0 through the last element?", clarification_options=["range(len(items))","range(1, len(items))","range(0, len(items)-1)","range(0, len(items)+1)"], expected_user_response="range(len(items))", corrected_code="for i in range(len(items)):\n    process(items[i])", explanation="range(1, len(items)) starts at index 1, skipping the first element. range(len(items)) starts at 0.", difficulty="easy", category="misc"),

  dict(id="case_127", buggy_code="squares = [x^2 for x in range(10)]", task="Create a list of squares of numbers 0-9", libs=["python"], error_type="wrong_list_comprehension", error_location="line 1: ^ is bitwise XOR, not exponentiation", clarification_query="Which operator computes the square (exponentiation) in Python?", clarification_options=["**","^","*","//"], expected_user_response="**", corrected_code="squares = [x**2 for x in range(10)]", explanation="In Python, ^ is the bitwise XOR operator. ** is the exponentiation operator.", difficulty="easy", category="misc"),

  dict(id="case_128", buggy_code="config = {'host': 'localhost', 'port': 5432}\nprint(config['database'])", task="Access the 'host' key from the config dictionary", libs=["python"], error_type="wrong_dict_key", error_location="line 2: 'database' key does not exist in config", clarification_query="Which key holds the server hostname in the config dict?", clarification_options=["'host'","'database'","'server'","'hostname'"], expected_user_response="'host'", corrected_code="config = {'host': 'localhost', 'port': 5432}\nprint(config['host'])", explanation="config has no 'database' key; accessing it raises KeyError. The correct key is 'host'.", difficulty="easy", category="misc"),

  dict(id="case_129", buggy_code="if score >= 90:\n    grade = 'A'\nelif score >= 70:\n    grade = 'B'\nelif score >= 80:\n    grade = 'C'", task="Assign grades: A for >=90, B for >=80, C for >=70", libs=["python"], error_type="wrong_conditional_order", error_location="line 4-5: >=80 check comes after >=70, so scores 80-89 get 'B' when they should", clarification_query="What is the correct order for the grade thresholds?", clarification_options=[">=90, >=80, >=70",">=90, >=70, >=80",">=80, >=90, >=70",">=70, >=80, >=90"], expected_user_response=">=90, >=80, >=70", corrected_code="if score >= 90:\n    grade = 'A'\nelif score >= 80:\n    grade = 'B'\nelif score >= 70:\n    grade = 'C'", explanation="elif chains are checked in order. >=70 before >=80 means scores 70-89 all become 'B' before the 80 check is reached.", difficulty="medium", category="misc"),

  dict(id="case_130", buggy_code="for item in items:\n    if item == target:\n        found = True\nprint(found)", task="Search a list and break out of the loop when the target is found", libs=["python"], error_type="missing_break", error_location="line 3: no break statement; loop continues unnecessarily", clarification_query="What statement exits the loop immediately after finding the target?", clarification_options=["break","continue","return","pass"], expected_user_response="break", corrected_code="for item in items:\n    if item == target:\n        found = True\n        break\nprint(found)", explanation="Without break, the loop continues through all elements even after the target is found. break exits immediately.", difficulty="easy", category="misc"),

  dict(id="case_131", buggy_code="def add_item(item, lst=[]):\n    lst.append(item)\n    return lst\n\nlist1 = add_item('a')\nlist2 = add_item('b')", task="Collect items in independent lists for each call (no shared default)", libs=["python"], error_type="wrong_default_mutable", error_location="line 1: mutable default argument [] is shared across calls", clarification_query="How should you avoid sharing a mutable default list across function calls?", clarification_options=["use lst=None and create list inside","use lst=[] but clear it","use lst=tuple()","use global instead"], expected_user_response="use lst=None and create list inside", corrected_code="def add_item(item, lst=None):\n    if lst is None:\n        lst = []\n    lst.append(item)\n    return lst\n\nlist1 = add_item('a')\nlist2 = add_item('b')", explanation="Default mutable arguments are created once and reused across calls. Using None as sentinel and creating a new list inside avoids sharing.", difficulty="medium", category="misc"),

  dict(id="case_132", buggy_code="count = 0\ndef increment():\n    count += 1\nincrement()", task="Increment a global counter variable from inside a function", libs=["python"], error_type="wrong_global_variable", error_location="line 3: count += 1 treats count as local without global declaration", clarification_query="What statement is needed to modify a global variable inside a function?", clarification_options=["global count","nonlocal count","extern count","count is global automatically"], expected_user_response="global count", corrected_code="count = 0\ndef increment():\n    global count\n    count += 1\nincrement()", explanation="Without 'global count', Python treats count as a local variable, raising UnboundLocalError on count += 1.", difficulty="medium", category="misc"),

  dict(id="case_133", buggy_code="def factorial(n):\n    if n == 0:\n        return 1\n    return n * factorial(n)", task="Compute factorial recursively with correct base case", libs=["python"], error_type="wrong_recursion_base", error_location="line 4: recursive call uses n instead of n-1", clarification_query="What argument should the recursive factorial call use?", clarification_options=["n - 1","n","n + 1","n // 2"], expected_user_response="n - 1", corrected_code="def factorial(n):\n    if n == 0:\n        return 1\n    return n * factorial(n - 1)", explanation="factorial(n) must call factorial(n-1) to decrease toward the base case n==0. Using n causes infinite recursion.", difficulty="easy", category="misc"),

  dict(id="case_134", buggy_code="total = int(3.7 + 2.3)", task="Convert the string '42' to an integer, then add 8", libs=["python"], error_type="wrong_type_conversion", error_location="line 1: int() converts float expression, not a string", clarification_query="How do you convert the string '42' to an integer?", clarification_options=["int('42')","float('42')","str(42)","ord('42')"], expected_user_response="int('42')", corrected_code="total = int('42') + 8", explanation="int(3.7 + 2.3) evaluates the float expression then truncates. To convert a string '42', use int('42').", difficulty="easy", category="misc"),

  dict(id="case_135", buggy_code="name = 'Alice'\nprint('Hello, %s. You are %d years old.' % (name))", task="Format a greeting with name and age using % formatting", libs=["python"], error_type="wrong_format_string", error_location="line 2: only one value in tuple but two format specifiers", clarification_query="How many values must be in the tuple when two format specifiers (%s and %d) are used?", clarification_options=["2 — one per specifier","1","3","any number"], expected_user_response="2 — one per specifier", corrected_code="name = 'Alice'\nage = 30\nprint('Hello, %s. You are %d years old.' % (name, age))", explanation="Two format specifiers (%s and %d) require a tuple with 2 values. Providing only one causes TypeError.", difficulty="easy", category="misc"),

  dict(id="case_136", buggy_code="import re\nmatches = re.findall(r'\\d+', 'Price: $42.50 and $8')", task="Extract decimal numbers including the decimal point (e.g., '42.50')", libs=["python","re"], error_type="wrong_regex_pattern", error_location="line 2: \\d+ matches only integer digits, not decimals", clarification_query="What regex pattern matches decimal numbers like 42.50?", clarification_options=["r'\\d+\\.?\\d*'","r'\\d+'","r'[0-9.]+'","r'\\d{1,5}'"], expected_user_response="r'\\d+\\.?\\d*'", corrected_code="import re\nmatches = re.findall(r'\\d+\\.?\\d*', 'Price: $42.50 and $8')", explanation="\\d+ matches only integer digits. \\d+\\.?\\d* matches integers and optionally a dot followed by more digits.", difficulty="medium", category="misc"),

  dict(id="case_137", buggy_code="f = open('data.txt', 'r')\ndata = f.read()\nprocess(data)", task="Read a file safely and ensure it is closed even if an error occurs", libs=["python"], error_type="missing_file_close", error_location="line 3: f.close() never called; no context manager used", clarification_query="What Python construct automatically closes a file when done?", clarification_options=["with open('data.txt') as f:","try/finally with f.close()","both are correct","f.autoclose = True"], expected_user_response="both are correct", corrected_code="with open('data.txt', 'r') as f:\n    data = f.read()\nprocess(data)", explanation="Without a context manager or explicit close, the file may stay open. 'with open(...)' guarantees closure even on exceptions.", difficulty="easy", category="misc"),

  dict(id="case_138", buggy_code="try:\n    result = int(user_input)\nexcept Exception:\n    print('Not a number')", task="Catch only ValueError when converting user input to int", libs=["python"], error_type="wrong_exception_type", error_location="line 3: except Exception catches all exceptions too broadly", clarification_query="Which exception type should be caught for a failed int() conversion?", clarification_options=["ValueError","Exception","TypeError","RuntimeError"], expected_user_response="ValueError", corrected_code="try:\n    result = int(user_input)\nexcept ValueError:\n    print('Not a number')", explanation="except Exception is too broad and may hide bugs. int() raises ValueError for non-numeric strings, so catch that specifically.", difficulty="medium", category="misc"),

  dict(id="case_139", buggy_code="def my_decorator(func):\n    def wrapper(*args, **kwargs):\n        return func(*args, **kwargs)\n    return wrapper\n\n@my_decorator\n@another_decorator\ndef greet():\n    pass", task="Apply decorators in the correct order: another_decorator wraps greet first, then my_decorator", libs=["python"], error_type="wrong_decorator_order", error_location="lines 6-7: @my_decorator is outermost so it's applied last", clarification_query="In Python, which decorator in a stack is applied first (closest to the function)?", clarification_options=["the one immediately above the function","the topmost decorator","they are applied simultaneously","order depends on decorator type"], expected_user_response="the one immediately above the function", corrected_code="@my_decorator\n@another_decorator\ndef greet():\n    pass", explanation="Decorators are applied bottom-up. @another_decorator (closest to def) wraps first; @my_decorator wraps the result.", difficulty="medium", category="misc"),

  dict(id="case_140", buggy_code="class Counter:\n    count = 0\n    def increment(self):\n        self.count += 1\n\na = Counter()\nb = Counter()\na.increment()\nprint(b.count)", task="Each Counter instance should track its own count independently", libs=["python"], error_type="wrong_class_attribute", error_location="line 2: count defined as class attribute, shared until instance shadow", clarification_query="How should count be defined so each instance has its own independent counter?", clarification_options=["in __init__: self.count = 0","as class attribute: count = 0","as a property","as a class method"], expected_user_response="in __init__: self.count = 0", corrected_code="class Counter:\n    def __init__(self):\n        self.count = 0\n    def increment(self):\n        self.count += 1\n\na = Counter()\nb = Counter()\na.increment()\nprint(b.count)", explanation="Class attributes are shared. self.count += 1 creates an instance attribute on a, but b.count still reads the class attribute. Initializing in __init__ ensures independence.", difficulty="medium", category="misc"),

  dict(id="case_141", buggy_code="result = 2 + 3 * 4 ** 2 - 1", task="Compute (2 + 3) * 4 ** 2 - 1 respecting intended precedence", libs=["python"], error_type="wrong_operator_precedence", error_location="line 1: missing parentheses cause wrong evaluation order", clarification_query="What parenthesization computes (2+3) first, then multiplies by 4**2?", clarification_options=["(2 + 3) * 4 ** 2 - 1","2 + 3 * 4 ** 2 - 1","2 + (3 * 4) ** 2 - 1","2 + 3 * (4 ** 2 - 1)"], expected_user_response="(2 + 3) * 4 ** 2 - 1", corrected_code="result = (2 + 3) * 4 ** 2 - 1", explanation="Without parentheses, Python evaluates ** first, then *, then + and - left-to-right. Parentheses around (2+3) change the order.", difficulty="medium", category="misc"),

  dict(id="case_142", buggy_code="every_other = items[::1]", task="Get every other element of a list starting from index 0", libs=["python"], error_type="wrong_slice_step", error_location="line 1: step=1 returns all elements, not every other", clarification_query="What slice step selects every other element?", clarification_options=["2","1","0","-1"], expected_user_response="2", corrected_code="every_other = items[::2]", explanation="[::1] is the same as [:] — it returns all elements with step 1. [::2] skips every other element.", difficulty="easy", category="misc"),

  dict(id="case_143", buggy_code="for name, score in zip(scores, names):\n    print(f'{name}: {score}')", task="Pair each name with its corresponding score (names list first, then scores)", libs=["python"], error_type="wrong_zip_order", error_location="line 1: zip order puts scores first, names second", clarification_query="In zip(scores, names), which variable receives the name and which the score in the loop?", clarification_options=["name gets score and score gets name (order is swapped)","name gets name and score gets score","zip reorders automatically","it depends on list length"], expected_user_response="name gets score and score gets name (order is swapped)", corrected_code="for name, score in zip(names, scores):\n    print(f'{name}: {score}')", explanation="zip(scores, names) yields (score, name) pairs. Unpacking to (name, score) assigns the score to name and vice versa.", difficulty="medium", category="misc"),

  dict(id="case_144", buggy_code="words = ['banana', 'apple', 'cherry']\nsorted_words = sorted(words, key=len, reverse=False)", task="Sort words by length in descending order (longest first)", libs=["python"], error_type="wrong_sorted_key", error_location="line 2: reverse=False sorts shortest first", clarification_query="What reverse value sorts by length with longest words first?", clarification_options=["reverse=True","reverse=False","key=-len","key=lambda w: -len(w)"], expected_user_response="reverse=True", corrected_code="sorted_words = sorted(words, key=len, reverse=True)", explanation="reverse=False (default) puts shortest first. reverse=True reverses the sort order so longest words come first.", difficulty="easy", category="misc"),

  dict(id="case_145", buggy_code="funcs = []\nfor i in range(3):\n    funcs.append(lambda: i)\nprint([f() for f in funcs])", task="Create a list of lambdas that capture values 0, 1, 2 independently", libs=["python"], error_type="wrong_lambda_scope", error_location="line 3: lambda captures i by reference, not by value", clarification_query="How do you capture the current value of i in a lambda inside a loop?", clarification_options=["lambda i=i: i","lambda: i","lambda x=i: x","use a closure with a nested function"], expected_user_response="lambda i=i: i", corrected_code="funcs = []\nfor i in range(3):\n    funcs.append(lambda i=i: i)\nprint([f() for f in funcs])", explanation="Closures capture variables by reference. When the loop ends i==2, all lambdas return 2. Default argument lambda i=i: i captures the value at definition time.", difficulty="hard", category="misc"),

  dict(id="case_146", buggy_code="total = sum(x*x for x in range(10) if x > 5)\nprint(list(total))", task="Compute the sum of squares for x > 5 and print the scalar result", libs=["python"], error_type="wrong_generator_expression", error_location="line 2: list(total) called on an integer (sum returns a scalar)", clarification_query="What does sum() return when given a generator expression?", clarification_options=["a scalar number","a generator","a list","a tuple"], expected_user_response="a scalar number", corrected_code="total = sum(x*x for x in range(10) if x > 5)\nprint(total)", explanation="sum() consumes the generator and returns a scalar. Wrapping an integer in list() raises TypeError.", difficulty="medium", category="misc"),

  dict(id="case_147", buggy_code="with open('a.txt') as f1, open('b.txt') as f2:\n    data = f1.read()\ndata2 = f2.read()", task="Read both files inside the context manager block", libs=["python"], error_type="wrong_context_manager", error_location="line 3: f2.read() called outside the with block after f2 is closed", clarification_query="Is f2 still open after the with block exits?", clarification_options=["no, both files are closed when the with block exits","yes, only f1 is closed","yes, both remain open","it depends on the OS"], expected_user_response="no, both files are closed when the with block exits", corrected_code="with open('a.txt') as f1, open('b.txt') as f2:\n    data = f1.read()\n    data2 = f2.read()", explanation="The with statement closes all managed files on exit. Reading f2 outside the block raises ValueError (I/O on closed file).", difficulty="medium", category="misc"),

  dict(id="case_148", buggy_code="items = [1, 2, 3]\nfor i, v in enumerate(items, start=0):\n    print(i, v)", task="Enumerate items starting the index counter from 1, not 0", libs=["python"], error_type="wrong_loop_range", error_location="line 2: start=0 (default) numbers from 0", clarification_query="What start value makes enumerate count from 1?", clarification_options=["start=1","start=0","start=None","enumerate doesn't support start"], expected_user_response="start=1", corrected_code="items = [1, 2, 3]\nfor i, v in enumerate(items, start=1):\n    print(i, v)", explanation="enumerate(items, start=0) produces (0,v),(1,v),(2,v). start=1 produces (1,v),(2,v),(3,v) for 1-based indexing.", difficulty="easy", category="misc"),

  dict(id="case_149", buggy_code="data = {'a': 1, 'b': 2}\nfor k in data:\n    del data[k]", task="Clear all entries from a dictionary by deleting keys during iteration", libs=["python"], error_type="wrong_dict_key", error_location="line 3: modifying dict size during iteration raises RuntimeError", clarification_query="How do you safely delete all entries from a dict?", clarification_options=["data.clear()","del data[k] after iterating a copy: for k in list(data)","both are valid approaches","use dict.pop() in a while loop"], expected_user_response="both are valid approaches", corrected_code="data.clear()\n# or: for k in list(data): del data[k]", explanation="Deleting keys while iterating the dict raises RuntimeError in Python 3. Use data.clear() or iterate over a copy with list(data).", difficulty="medium", category="misc"),

  dict(id="case_150", buggy_code="result = []\nfor x in range(5):\n    for y in range(5):\n        if x != y:\n            result.append((x,y))\n        else:\n            continue", task="Build pairs (x,y) where x != y; the else/continue is redundant", libs=["python"], error_type="logic_inversion", error_location="lines 6-7: else: continue is a no-op (loop continues anyway)", clarification_query="Is the else: continue block necessary when it's the last statement in the if/else?", clarification_options=["no, continue is the default loop behavior","yes, it makes the intent explicit","yes, without it x==y pairs would be included","no, but it prevents the append"], expected_user_response="no, continue is the default loop behavior", corrected_code="result = [(x,y) for x in range(5) for y in range(5) if x != y]", explanation="After an if block, the loop continues to the next iteration automatically. else: continue adds no effect and can be removed.", difficulty="medium", category="misc"),
]

for case in CASES:
    path = OUT / f"{case['id']}.json"
    path.write_text(json.dumps(case, indent=2))
    print(f"wrote {path.name}")

print(f"\nTotal cases written: {len(CASES)}")
