batch_size: 8
learning_rate: 5.0e-5
num_epochs: 3
optimizer: adamw
warmup_steps: 100
max_seq_len: 1024
lora:
  enabled: true
  r: 16
  alpha: 32
  target_modules: ["q_proj", "v_proj"]
  dropout: 0.05
output_dir: results/checkpoints
